## Packages
framer-motion | Page transitions and smooth micro-interactions
date-fns | Human-readable date formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-body)"],
}
Dashboard requires dummy images; using Unsplash placeholders.
Registration action is UI-only for now as per backend schema limits.
